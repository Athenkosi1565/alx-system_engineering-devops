#!/bin/bash

# Split the PATH variable using the ':' delimiter and store the result in an array
IFS=':' read -ra PATH_DIRS <<< "$PATH"

# Count the number of directories
COUNT=${#PATH_DIRS[@]}

# Print the count
echo "$COUNT"
